import { LabIcon } from '@jupyterlab/ui-components';

import compss_svg from '../../resources/compss.svg';

const compss_icon = new LabIcon({
  name: 'ipycompss_lab_extension:compss',
  svgstr: compss_svg
});

export default compss_icon;
